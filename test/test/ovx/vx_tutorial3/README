copy cl_viv_vx_ext.h, left_gray.bmp, right_gray.bmp and vx_tutorial3 into the same direcotry

execute vx_tutorial3 without extra parameters

if everything goes well, a absdiff_out.bmp will be generated in the same direcotry
