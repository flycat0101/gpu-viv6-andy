/****************************************************************************
*
*    Copyright (c) 2005 - 2019 by Vivante Corp.  All rights reserved.
*
*    The material in this file is confidential and contains trade secrets
*    of Vivante Corporation. This is proprietary information owned by
*    Vivante Corporation. No part of this work may be disclosed,
*    reproduced, copied, transmitted, or used in any way for any purpose,
*    without the express written permission of Vivante Corporation.
*
*****************************************************************************/


/*
 * Vivante Gralloc Allocator header file.
 * Exported for gralloc module.
 *
 * Please DO NOT EDIT this file.
 */

#ifndef __gc_gralloc_h_
#define __gc_gralloc_h_

#include <hardware/hardware.h>
#include <hardware/gralloc.h>
#include "gc_gralloc_helper.h"

struct private_module_t;
struct private_handle_t;

#ifdef __cplusplus
extern "C" {
#endif


/* alloc device functions. */
int
gc_gralloc_alloc(
    struct alloc_device_t * dev,
    int width,
    int height,
    int format,
    int usage,
    buffer_handle_t * handle,
    int * stride
    );

int
gc_gralloc_free(
    struct alloc_device_t * dev,
    buffer_handle_t handle
    );

void
gc_gralloc_dump(
    struct alloc_device_t *dev,
    char *buff,
    int buff_len
    );

/* module functions. */
int
gc_gralloc_register_buffer(
    struct gralloc_module_t const * module,
    buffer_handle_t handle
    );

int
gc_gralloc_unregister_buffer(
    struct gralloc_module_t const * module,
    buffer_handle_t handle
    );

int
gc_gralloc_lock(
    struct gralloc_module_t const * module,
    buffer_handle_t handle,
    int usage,
    int left,
    int top,
    int width,
    int height,
    void ** vaddr
    );

int
gc_gralloc_unlock(
    struct gralloc_module_t const * module,
    buffer_handle_t handle
    );

int
gc_gralloc_perform(
    struct gralloc_module_t const * module,
    int operation,
    ...
    );

#if ANDROID_SDK_VERSION >= 18
int
gc_gralloc_lock_ycbcr(
    struct gralloc_module_t const* module,
    buffer_handle_t handle,
    int usage,
    int left,
    int top,
    int width,
    int height,
    struct android_ycbcr *ycbcr
    );
#endif

#ifdef __cplusplus
}
#endif

#endif /* __gc_gralloc_h_ */

